# Anthropic API key for the AI Underwriter (Claude Sonnet 5).
# Copy this file to `.env` in the same directory (backend/.env) and paste your real key.
# The `.env` file is git-ignored and its value is never logged.
ANTHROPIC_API_KEY=sk-ant-...
